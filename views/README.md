# Views

This folder can contain **generated summaries** derived from `ltm/long_term_memory.jsonl` to make the memory set easier to browse.

These files are optional and may be regenerated by scripts.
